server:
  port: 8091

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/wiki_server
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root